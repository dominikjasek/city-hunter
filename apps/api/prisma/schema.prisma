generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           Int           @id @default(autoincrement())
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt @default(now())
  thirdPartyId String
  email        String
  photoUrl     String
  firstName    String
  lastName     String
  provider     LoginProvider
  refreshToken String        @default("default-value-by-prisma")

  @@map("users")
}

enum LoginProvider {
  GOOGLE
}
